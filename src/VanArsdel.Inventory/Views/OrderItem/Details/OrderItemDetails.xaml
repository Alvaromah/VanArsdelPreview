<UserControl
    x:Class="VanArsdel.Inventory.Views.OrderItemDetails"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:VanArsdel.Inventory.Controls"
    xmlns:viewmodels="using:VanArsdel.Inventory.ViewModels"
    mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600">

    <UserControl.Resources>
        <DataTemplate x:Key="DetailsTemplate" x:DataType="viewmodels:OrderItemDetailsViewModel">
            <Grid ColumnSpacing="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="240"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Margin="6" Spacing="4">
                    <Image Margin="3" Source="{x:Bind Item.Product.PictureBitmap, Mode=OneWay}" MinWidth="240" />
                    <TextBlock Text="{x:Bind Item.Product.Name, Mode=OneWay}" TextAlignment="Center" TextWrapping="WrapWholeWords" HorizontalAlignment="Stretch" FontSize="16" Foreground="{ThemeResource SystemControlForegroundAccentBrush}" IsTextSelectionEnabled="True" />
                    <TextBlock Text="{x:Bind Item.Product.ProductID, Mode=OneWay}" TextAlignment="Center" HorizontalAlignment="Stretch" IsTextSelectionEnabled="True" />
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="6">
                    <controls:LabelTextBox Label="Quantity" Text="{x:Bind Item.Quantity, Mode=TwoWay, Converter={StaticResource Int32Converter}}" ValueType="Int32" />
                    <controls:LabelTextBlock Label="Unit Price" Text="{x:Bind Item.UnitPrice, Mode=OneWay, Converter={StaticResource DecimalConverter}}" ValueType="Decimal" />
                </StackPanel>

                <StackPanel Grid.Column="2" Spacing="6">
                    <controls:LabelTextBox Label="Discount" Text="{x:Bind Item.Discount, Mode=TwoWay, Converter={StaticResource DecimalConverter}}" ValueType="Decimal" />
                    <controls:LabelTextBlock Label="Subtotal" Text="{x:Bind Item.Subtotal, Mode=OneWay, Converter={StaticResource DecimalConverter}}" ValueType="Decimal" />
                </StackPanel>

                <StackPanel Grid.Column="3" Spacing="6">
                    <controls:LabelComboBox Label="Tax Type" ItemsSource="{x:Bind DataHelper.TaxTypes}"
                                            SelectedValue="{x:Bind Item.TaxType, Mode=TwoWay, Converter={StaticResource Int32Converter}}"
                                            SelectedValuePath="TaxTypeID" DisplayMemberPath="Name" />
                    <controls:LabelTextBlock Label="Total" Text="{x:Bind Item.Total, Mode=OneWay, Converter={StaticResource DecimalConverter}}" ValueType="Decimal" />
                </StackPanel>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <controls:Details x:Name="details" Margin="12,0,0,0"
                      DetailsContent="{x:Bind ViewModel}"
                      DetailsTemplate="{StaticResource DetailsTemplate}"
                      CanGoBack="{x:Bind ViewModel.CanGoBack}"
                      IsEditMode="{x:Bind ViewModel.IsEditMode, Mode=OneWay}"
                      BackCommand="{x:Bind ViewModel.BackCommand}"
                      EditCommand="{x:Bind ViewModel.EditCommand}"
                      DeleteCommand="{x:Bind ViewModel.DeleteCommand}"
                      SaveCommand="{x:Bind ViewModel.SaveCommand}"
                      CancelCommand="{x:Bind ViewModel.CancelCommand}" />
</UserControl>
